{
  "config": {
    "step": {
      "user": {
        "title": "Set up Fade Lights",
        "description": "This integration provides smooth light fading and automatic brightness adjustment for dimmed lights."
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure Fade Lights",
        "description": "Configure default behavior for light fading and automatic brightness adjustment.",
        "data": {
          "default_brightness_pct": "Default brightness percentage",
          "default_transition": "Default transition time (seconds)",
          "auto_brightness_threshold": "Auto-brightness threshold percentage",
          "auto_brightness_target": "Auto-brightness target percentage"
        },
        "data_description": {
          "default_brightness_pct": "Default brightness level (0-100%) used when fade_lights service is called without specifying brightness_pct.",
          "default_transition": "Default transition time in seconds (0-3600) used when fade_lights service is called without specifying transition.",
          "auto_brightness_threshold": "When a light is turned on with brightness below this percentage (0-100%), it will automatically be adjusted to the target brightness.",
          "auto_brightness_target": "Target brightness percentage (0-100%) to set when auto-brightness is triggered."
        }
      }
    }
  },
  "services": {
    "fade_lights": {
      "name": "Fade lights",
      "description": "Fades lights to a desired level over a specified transition period.",
      "fields": {
        "entity_id": {
          "name": "Entity ID",
          "description": "Entity ID of a light or light group."
        },
        "brightness_pct": {
          "name": "Brightness percentage",
          "description": "Integer value from 0 to 100 representing the desired final brightness level as a percentage."
        },
        "transition": {
          "name": "Transition",
          "description": "Transition time for fading indicated in HH:MM:SS, MM:SS, or SS format (e.g., 00:05:30 or 5)."
        },
        "force": {
          "name": "Force",
          "description": "Set to true to force changing the brightness even if it has been changed manually."
        }
      }
    }
  }
}
